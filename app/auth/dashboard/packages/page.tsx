'use client';
import { useEffect, useMemo, useState } from 'react';
import { Plus, Save, Trash2, Search, MapPin, Clock3, Star, X, ChevronDown, ChevronUp } from 'lucide-react';
import AdminShell from '@/components/admin/AdminShell';
import MediaPicker from '@/components/admin/MediaPicker';
import { useAdminData, formatKES } from '@/components/admin/useAdminData';
import { safaris, type Safari } from '@/lib/tours-data';

type Form=Safari;
const blank:Form={id:'new-program',name:'New Safari Program',tagline:'',days:1,nights:0,parks:[],lodges:[],tabs:['tsavo'],rating:5,reviewCount:0,image:'',category:'short',highlights:[],itinerary:[{day:'Day 1',title:'Arrival / Safari',morning:'',afternoon:'',overnight:''}],packingTips:[],included:[],excluded:[],activityLevel:3,comfortLevel:4};
const split=(s:string)=>s.split(/\n|,/).map(x=>x.trim()).filter(Boolean);
const join=(a:string[]|undefined)=>Array.isArray(a)?a.join('\n'):'';

export default function PackagesPage(){
 const {stats}=useAdminData(); const [programs,setPrograms]=useState<Safari[]>([]); const [q,setQ]=useState(''); const [locale,setLocale]=useState('en'); const [selected,setSelected]=useState<Form|null>(null); const [busy,setBusy]=useState(false); const [msg,setMsg]=useState(''); const [openItinerary,setOpenItinerary]=useState<number|null>(0);
 const load=async()=>{const r=await fetch(`/api/admin/programs?locale=${locale}`);const d=await r.json();if(d.success)setPrograms([...(d.programs||[]),...(d.custom||[])])}; useEffect(()=>{load()},[locale]);
 const visible=useMemo(()=>programs.filter(s=>[s.name,s.tagline,...s.parks].join(' ').toLowerCase().includes(q.toLowerCase())),[programs,q]);
 const update=(k:keyof Form,v:any)=>setSelected(p=>p?({...p,[k]:v}):p);
 const save=async()=>{if(!selected)return;setBusy(true);setMsg('');try{const r=await fetch('/api/admin/programs',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({...selected,locale})});const d=await r.json();if(!r.ok||!d.success)throw new Error(d.error||'Save failed');setMsg('Program saved successfully.');await load();setSelected(null)}catch(e){setMsg(e instanceof Error?e.message:'Save failed')}finally{setBusy(false)}};
 const remove=async()=>{if(!selected||selected.id==='new-program')return setSelected(null);if(!confirm(`Delete the ${selected.name} program for ${locale}?`))return;setBusy(true);try{const r=await fetch('/api/admin/programs',{method:'DELETE',headers:{'Content-Type':'application/json'},body:JSON.stringify({slug:selected.id,locale})});const d=await r.json();if(!r.ok||!d.success)throw new Error(d.error||'Delete failed');setSelected(null);await load()}catch(e){setMsg(e instanceof Error?e.message:'Delete failed')}finally{setBusy(false)}};
 return <AdminShell title="Programs & Itineraries" badge={stats?.pending}><div className="space-y-5"><div className="flex flex-col gap-3 lg:flex-row lg:items-end lg:justify-between"><div><p className="text-xs font-bold uppercase tracking-[.16em] text-cyan-700">Product management</p><h2 className="text-2xl font-bold">Safari Programs</h2><p className="text-sm text-slate-500">Create, edit and publish program details and day-by-day itineraries.</p></div><div className="flex gap-2"><select value={locale} onChange={e=>setLocale(e.target.value)} className="rounded-xl border bg-white px-3 py-2.5 text-sm"><option value="en">English</option><option value="it">Italiano</option><option value="fr">Français</option><option value="es">Español</option><option value="de">Deutsch</option><option value="ar">العربية</option><option value="zh">中文</option><option value="sw">Kiswahili</option></select><button onClick={()=>setSelected({...blank,id:`new-${Date.now()}`})} className="rounded-xl bg-cyan-700 px-4 py-2.5 text-sm font-semibold text-white"><Plus className="mr-2 inline h-4 w-4"/>New program</button></div></div>
 <div className="flex gap-3"><div className="relative flex-1"><Search className="absolute left-3 top-1/2 h-4 w-4 -translate-y-1/2 text-slate-400"/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search programs..." className="w-full rounded-xl border bg-white py-3 pl-9 pr-3 text-sm"/></div>{msg&&<div className="rounded-xl bg-slate-900 px-4 py-3 text-xs text-white">{msg}</div>}</div>
 <div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{visible.map(s=><button key={s.id} onClick={()=>setSelected({...s})} className="rounded-2xl border bg-white p-5 text-left shadow-sm hover:shadow-md"><div className="flex justify-between gap-3"><div><h3 className="font-bold">{s.name}</h3><p className="mt-1 text-xs text-slate-500">{s.tagline}</p></div><span className="text-xs font-semibold"><Star className="mr-1 inline h-3.5 w-3.5"/>{s.rating}</span></div><div className="mt-4 flex flex-wrap gap-2 text-xs text-slate-500"><span><Clock3 className="mr-1 inline h-3 w-3"/>{s.days}D/{s.nights}N</span><span><MapPin className="mr-1 inline h-3 w-3"/>{s.parks.join(', ')}</span></div><div className="mt-4 border-t pt-3 text-xs text-cyan-700">Edit program & itinerary →</div></button>)}</div></div>
 {selected&&<div className="fixed inset-0 z-[70] bg-slate-950/50 p-3 sm:p-6"><div className="mx-auto flex h-full max-w-5xl flex-col overflow-hidden rounded-2xl bg-white shadow-2xl"><div className="flex items-center justify-between border-b p-5"><div><p className="text-xs uppercase tracking-wider text-cyan-700">{locale} program editor</p><h3 className="text-xl font-bold">{selected.id.startsWith('new-')?'Create program':selected.name}</h3></div><button onClick={()=>setSelected(null)}><X/></button></div><div className="flex-1 overflow-y-auto p-5"><div className="grid gap-4 md:grid-cols-2"><label className="text-xs font-semibold">Program name<input value={selected.name} onChange={e=>update('name',e.target.value)} className="mt-1 w-full rounded-lg border p-2.5 text-sm"/></label><label className="text-xs font-semibold">Tagline<input value={selected.tagline} onChange={e=>update('tagline',e.target.value)} className="mt-1 w-full rounded-lg border p-2.5 text-sm"/></label><label className="text-xs font-semibold">Slug<input value={selected.id} onChange={e=>update('id',e.target.value)} disabled={!selected.id.startsWith('new-')} className="mt-1 w-full rounded-lg border p-2.5 text-sm disabled:bg-slate-50"/></label><div className="text-xs font-semibold md:col-span-2"><span>Main image</span><div className="mt-1"><MediaPicker value={selected.image} onChange={url=>update('image',url)} category="safaris"/></div></div><label className="text-xs font-semibold">Days<input type="number" min="1" value={selected.days} onChange={e=>update('days',Number(e.target.value))} className="mt-1 w-full rounded-lg border p-2.5 text-sm"/></label><label className="text-xs font-semibold">Nights<input type="number" min="0" value={selected.nights} onChange={e=>update('nights',Number(e.target.value))} className="mt-1 w-full rounded-lg border p-2.5 text-sm"/></label><label className="text-xs font-semibold">Category<select value={selected.category} onChange={e=>update('category',e.target.value)} className="mt-1 w-full rounded-lg border p-2.5 text-sm"><option value="short">Short</option><option value="medium">Medium</option><option value="long">Long</option></select></label><label className="text-xs font-semibold">Rating<input type="number" min="0" max="5" step="0.1" value={selected.rating} onChange={e=>update('rating',Number(e.target.value))} className="mt-1 w-full rounded-lg border p-2.5 text-sm"/></label><label className="text-xs font-semibold">Review count<input type="number" min="0" value={selected.reviewCount} onChange={e=>update('reviewCount',Number(e.target.value))} className="mt-1 w-full rounded-lg border p-2.5 text-sm"/></label><label className="text-xs font-semibold">Activity level<input type="number" min="1" max="5" value={selected.activityLevel||3} onChange={e=>update('activityLevel',Number(e.target.value))} className="mt-1 w-full rounded-lg border p-2.5 text-sm"/></label><label className="text-xs font-semibold">Comfort level<input type="number" min="1" max="5" value={selected.comfortLevel||4} onChange={e=>update('comfortLevel',Number(e.target.value))} className="mt-1 w-full rounded-lg border p-2.5 text-sm"/></label><label className="flex items-center gap-2 text-xs font-semibold"><input type="checkbox" checked={!!selected.popular} onChange={e=>update('popular',e.target.checked)}/> Featured / popular</label><label className="text-xs font-semibold">Parks / destinations<textarea value={join(selected.parks)} onChange={e=>update('parks',split(e.target.value))} rows={3} className="mt-1 w-full rounded-lg border p-2.5 text-sm"/></label><label className="text-xs font-semibold">Accommodation / lodges<textarea value={join(selected.lodges)} onChange={e=>update('lodges',split(e.target.value))} rows={3} className="mt-1 w-full rounded-lg border p-2.5 text-sm"/></label><label className="text-xs font-semibold md:col-span-2">Highlights<textarea value={join(selected.highlights)} onChange={e=>update('highlights',split(e.target.value))} rows={3} className="mt-1 w-full rounded-lg border p-2.5 text-sm"/></label><label className="text-xs font-semibold md:col-span-2">Packing tips<textarea value={join(selected.packingTips)} onChange={e=>update('packingTips',split(e.target.value))} rows={3} className="mt-1 w-full rounded-lg border p-2.5 text-sm"/></label></div>
 <div className="mt-6 flex items-center justify-between"><h4 className="font-bold">Day-by-day itinerary</h4><button onClick={()=>update('itinerary',[...selected.itinerary,{day:`Day ${selected.itinerary.length+1}`,title:'New day',morning:'',afternoon:'',overnight:''}])} className="rounded-lg border px-3 py-2 text-xs font-semibold"><Plus className="mr-1 inline h-3.5 w-3.5"/>Add day</button></div><div className="mt-3 space-y-3">{selected.itinerary.map((d,i)=><div key={i} className="rounded-xl border"><button onClick={()=>setOpenItinerary(openItinerary===i?null:i)} className="flex w-full items-center justify-between p-4 text-left"><span className="font-semibold">{d.day} — {d.title}</span>{openItinerary===i?<ChevronUp/>:<ChevronDown/>}</button>{openItinerary===i&&<div className="grid gap-3 border-t p-4 md:grid-cols-2"><label className="text-xs font-semibold">Day<input value={d.day} onChange={e=>{const a=[...selected.itinerary];a[i]={...d,day:e.target.value};update('itinerary',a)}} className="mt-1 w-full rounded-lg border p-2.5 text-sm"/></label><label className="text-xs font-semibold">Title<input value={d.title} onChange={e=>{const a=[...selected.itinerary];a[i]={...d,title:e.target.value};update('itinerary',a)}} className="mt-1 w-full rounded-lg border p-2.5 text-sm"/></label>{(['morning','afternoon','overnight'] as const).map(k=><label key={k} className="text-xs font-semibold md:col-span-2">{k}<textarea value={d[k]} onChange={e=>{const a=[...selected.itinerary];a[i]={...d,[k]:e.target.value};update('itinerary',a)}} rows={3} className="mt-1 w-full rounded-lg border p-2.5 text-sm"/></label>)}<button onClick={()=>update('itinerary',selected.itinerary.filter((_,n)=>n!==i))} className="text-left text-xs font-semibold text-red-600"><Trash2 className="mr-1 inline h-3.5 w-3.5"/>Remove day</button></div>}</div>)}</div>
 <div className="mt-6 grid gap-4 md:grid-cols-2"><label className="text-xs font-semibold">Included<textarea value={join(selected.included)} onChange={e=>update('included',split(e.target.value))} rows={4} className="mt-1 w-full rounded-lg border p-2.5 text-sm"/></label><label className="text-xs font-semibold">Excluded<textarea value={join(selected.excluded)} onChange={e=>update('excluded',split(e.target.value))} rows={4} className="mt-1 w-full rounded-lg border p-2.5 text-sm"/></label></div></div><div className="flex items-center justify-between border-t bg-slate-50 p-4"><button onClick={remove} disabled={busy||selected.id.startsWith('new-')} className="rounded-lg border border-red-200 px-4 py-2.5 text-sm font-semibold text-red-600 disabled:opacity-40"><Trash2 className="mr-2 inline h-4 w-4"/>Delete locale</button><div className="flex gap-2"><button onClick={()=>setSelected(null)} className="rounded-lg border px-4 py-2.5 text-sm">Cancel</button><button onClick={save} disabled={busy} className="rounded-lg bg-cyan-700 px-5 py-2.5 text-sm font-semibold text-white"><Save className="mr-2 inline h-4 w-4"/>{busy?'Saving...':'Save & publish'}</button></div></div></div></div>}
 </AdminShell>
}
