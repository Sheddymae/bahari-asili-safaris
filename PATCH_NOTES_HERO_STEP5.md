# Hero hydration safety — Step 5

Hydration hardening is isolated to the cinematic Hero. The first render uses the locale already resolved by `LanguageProvider`; browser-only state is activated in effects.
