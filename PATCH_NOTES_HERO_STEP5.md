# Hero hydration safety — Step 5

The cinematic Hero uses the locale already resolved by `LanguageProvider` for its first render. Browser-only state is activated in effects, with no `localStorage`, `window`, or `document` access during render.
